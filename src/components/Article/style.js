import styled from "styled-components"

export const Container = styled.article`
    width: 232px; height: 280px;
    overflow: hidden;

    border-radius: 24px;
    background-color: #0D1B2A;
    color: #fff;

    & div { padding: 16px; }
    & div p { margin: 8px 0; }
    & div a { color: #fff; text-decoration: underline;}
`